name: nsg_generator
description: A simple command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  # Финализировано постановщиком: фиксируем Dart 3.10+. Все актуальные потребители
  # (FUTBOLISTA, nsg_discounts_app и др.) уже на 3.10+.
  # Baseline schema hash (84af1356e618e48b на FootballersDiary GeneratorConfig)
  # посчитан под этим SDK; при апгрейде верхней границы или смене канонизации
  # — обязательная сверка baseline-а (см. TASK01.md, §«Чек-лист совместимости»).
  sdk: '>=3.10.0 <4.0.0'


dependencies:
  path: ^1.8.2
  unorm_dart: ^0.3.0

dev_dependencies:
  test: ^1.21.0